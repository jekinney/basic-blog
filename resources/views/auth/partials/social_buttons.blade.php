<div class="row">
    <div class="col-xs-12">
        <div class="text-center">
            <a href="{{ route('social.redirect', 'facebook') }}" class="btn btn-primary">Facebook</a>
            <a href="{{ route('social.redirect', 'github') }}" class="btn btn-info">Github</a>
            <a href="{{ route('social.redirect', 'google') }}" class="btn btn-danger">Google +</a>
        </div>
    </div>
</div>